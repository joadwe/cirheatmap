<?xml version="1.0" encoding="utf-8"?>
<demos><!--This is an autogenerated file, please do not modify-->
   <name>CirHeatmap</name>
   <type>toolbox</type>
   <icon>HelpIcon.DEMOS</icon>
   <website/>
   <description>A function that allows plotting of a data array in a circular heatmap. 
Optionally spatially separating groups can be achieved by using a cell array with a data array of each group per cell.

Allows input for the cmocean toolbox colormaps on the file exchange.

Example: 
close all
clear

%% Online examples

DataX{1} = rand(100, 10); % Create dataset of 3 groups
DataX{2} = -rand(100, 5);
DataX{3} = rand(100, 7);

x = 1:1:100;
Labels = [];

for i = 1:numel(x)
    Labels{i} = ['Label ', num2str(x(i))]; % Create labels for each column
end

Groups = {'Pre', 'During','After'}; % Create labels for each group

[Fig] = CirHeatmap(DataX', 'GroupLabels', Groups,'OuterLabels', Labels, 'CircType', 'half','InnerSpacerSize',0.5);</description>
   <demosection>
      <label>Version 5</label>
      <demoitem>
         <label>Test_Run</label>
         <type>other</type>
         <source>Test_Run</source>
         <file>html/Test_Run.html</file>
      </demoitem>
   </demosection>
</demos>